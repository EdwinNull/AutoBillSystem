# 汽修店记账软件依赖包

# 数据处理
pandas>=1.3.0
numpy>=1.21.0

# 报表生成（可选）
reportlab>=3.6.0

# 日期时间处理
python-dateutil>=2.8.0

# 数据验证（可选）
cerberus>=1.3.0

# 配置文件处理（可选）
pyyaml>=5.4.0

# 日志记录（可选）
loguru>=0.6.0

# 注意：
# - tkinter 是Python内置模块，无需安装
# - sqlite3 是Python内置模块，无需安装
# - 本项目主要使用Python标准库，外部依赖较少
# - 标记为"可选"的包可以根据需要安装